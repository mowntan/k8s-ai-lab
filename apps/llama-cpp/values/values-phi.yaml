fullnameOverride: llama-phi

model:
  path: /models/microsoft_Phi-4-mini-instruct-Q4_0.gguf
  name: "Reasoning (Phi-4-mini)"
  contextSize: 4096

speculative:
  enabled: true
  specType: "ngram-simple"

tolerations:
  - key: node.longhorn.io/storage
    operator: Exists
    effect: NoSchedule
  - key: dedicated-node
    operator: Equal
    value: inference
    effect: NoSchedule

nodeSelector:
  kubernetes.io/hostname: pi4-node02.home.mowntan.com

persistence:
  models:
    nfs:
      server: synology-nas01.home.mowntan.com
      path: /volume2/downloads/models
